Korby website — app screenshots go here
=======================================

Drop these 5 PNG files in THIS folder, named EXACTLY as below. The landing page
already has a labeled slot for each. Once they're here, tell me and I'll swap the
placeholders for the real images and deploy.

  home.png       → Home screen: the "Next up" photo card + the year-so-far progress
  lists.png      → The three lists (Planned / Someday / Dreams)
  plan.png       → Planning a wish: the detail view with date, photo, checklist
  memories.png   → Memories: the shared timeline / "trail"
  together.png   → Pairing screen (the 6-character code) — optional but nice

Format:
  • Portrait iPhone screenshots, PNG.
  • Ideal: 6.7-inch (1290 × 2796). Any modern iPhone portrait size is fine —
    the site crops to fit.
  • From the Xcode Simulator: File → Save Screen (⌘S), or Device → Trigger Screenshot.
    From an iPhone: the usual screenshot, then AirDrop/upload here.

Tip: capture the redesigned (wine/ivory) build so the site matches the app.
